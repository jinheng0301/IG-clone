import 'package:flutter/material.dart';

class MessageScreen extends StatelessWidget {
  // final String uid;
  // MessageScreen({required this.uid});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: const Center(
        child: Text('Message screen'),
      ),
    );
  }
}
